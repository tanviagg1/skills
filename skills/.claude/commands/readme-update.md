Update the README to reflect recent changes in the codebase.

Follow these steps:

1. Read the existing README.md.

2. Run `git log --oneline -20` to see recent changes.

3. Read any files that were significantly changed to understand new features or removed ones.

4. Identify what in the README is now outdated or missing:
   - New features not mentioned
   - Setup or install steps that changed
   - Commands that changed
   - Removed features still documented

5. Update the README with accurate information. Do not rewrite sections that are still correct.

6. Show a diff of what changed.
